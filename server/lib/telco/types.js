/**
 * @typedef {Object} SmsSendParams
 * @property {string} to
 * @property {string} [from]
 * @property {string} text
 * @property {string} [clientRef]
 */
export {}; // keep file ESM + avoid “no exports” warnings
