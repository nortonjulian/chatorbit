{
  "name": "chatorbit-monorepo",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "lint": "eslint \"{client,server}/**/*.{js,jsx}\"",
    "lint:fix": "eslint \"{client,server}/**/*.{js,jsx}\" --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm -w client test && npm -w server test",
    "test:client": "npm -w client test",
    "test:server": "npm -w server test"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "eslint": "^9.33.0",
    "vitest": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "@vitest/coverage-v8": "^3.2.4",
    "supertest": "^7.1.4"
  }
}
